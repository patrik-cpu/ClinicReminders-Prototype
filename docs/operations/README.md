# Operations Docs

Operational documents live here so the repository root stays focused on source, tests, scripts, and release entry points.

- `BACKUP_RESTORE_RUNBOOK.md` - backup, restore, and recovery procedures for Sheets/Drive data.
- `GOOGLE_AUTH_SETUP.md` - local and deployed Google sign-in setup notes.

Release-specific gates remain in the root `PRE_PRODUCTION_CHECKLIST.md` because the local pilot-release tests intentionally verify that path.
